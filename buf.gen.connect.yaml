# Template for generating gRPC Connect stubs.
# Services to include are controlled via --path flags in build.sh,
# NOT here. Add new services by appending --path flags there.
version: v2
plugins:
  - remote: buf.build/connectrpc/go
    out: generated/go
    opt: paths=source_relative